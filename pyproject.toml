[project]
name = "polymarket-scanner"
version = "0.1.0"
description = "CLI tool to scan Polymarket markets for pricing inconsistencies"
requires-python = ">=3.11"
dependencies = []

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "-q"
